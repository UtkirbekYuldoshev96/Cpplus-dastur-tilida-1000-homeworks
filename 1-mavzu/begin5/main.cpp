#include <iostream>

using namespace std;

int main()
{
    double a, V, S;
    cout<<"Kubning yon tomonini kiriting = "; cin>>a;

    V = a * a *a;
    S = 6 * (a * a);

    cout<<"Uning hajmi = "<< V << endl;
    cout<<"To'la sirti = "<< S << endl;
    return 0;
}
