#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double a,b;
    cout<<"a = "; cin>>a;
    cout<<"b = "; cin>>b;

    cout<<"a + b / 2 = "<<a + b / 2 << endl;
    cout<<"sqrt(a*b) = "<<sqrt(a*b) << endl;


    return 0;
}
