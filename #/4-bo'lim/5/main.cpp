#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double a, V, S;
    cout<<"a = "; cin>>a;
    V= pow(a,3);
    S= 6*pow(a,2);

    cout<<"Hajm = "<< V << endl;
    cout<<"Yuza = "<< S << endl;

    return 0;
}
